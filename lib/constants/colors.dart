import 'package:flutter/material.dart';

// Light theme colors
class LightColors {
  static const background = Color(0xFFF5F5F5);
  static const primary = Colors.blueAccent;
  static const accent = Colors.orangeAccent;
  static const text = Colors.black;
  static const board = Color(0xFFFFFFFF);
}

// Dark theme colors
class DarkColors {
  static const background = Color(0xFF222831);
  static const primary = Colors.tealAccent;
  static const accent = Colors.deepOrangeAccent;
  static const text = Colors.white;
  static const board = Color(0xFF393E46);
}
